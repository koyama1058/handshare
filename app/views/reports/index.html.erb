<%= render 'shared/header' %>

<%# アイキャッチ画像 %>
    <%= image_tag "handshare_top.jpg", class:"d-block w-100" %>

<%# 投稿検索フォーム %>
  <div class="m-3 text-center">
    <h3 class="mb-3">投稿検索</h3>
    <%# <%= search_form_for @search, url: posts_search_path do |f| %> %>
      <%# <%= f.label :prefectures_id_eq, '都道府県', class:"mb-3" %> %>
      <%# <%= f.collection_select(:prefectures_id, Prefectures.all, :id, :name, {}) %> %>
      <%# <%= f.label :tag_eq, 'タグ' %>
      <%# <%= f.collection_select :name_eq, @post, :name, :name,  include_blank: '指定なし' %> 
      <br>
      <%# <%= f.submit '検索'%> %>
    <%# <% end %> %>
  </div>

<div class="m-3 text-center">
    <h3 class="mb-3 mt-5 text-center">最新レポ一覧</h3>
    <% @reports.each do |report| %>
    <div class="border border-success" >
      <%=link_to report.text, report_path(report.id), class:"card-link" %>
      <%=link_to report.sheets, report_path(report.id), class:"card-link" %>
      <%=link_to  MemberName.find(report.member_name_id).name, report_path(report.id), class:"card-link" %>
    </div>
    <% end %>
</div> 